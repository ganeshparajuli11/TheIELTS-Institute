# Styles

Global Tailwind v4 theme tokens live in `src/app/globals.css` because this is the current shadcn/ui and Next.js setup.

Use this folder for future style notes only. Avoid adding legacy Tailwind v3 configuration unless a later dependency explicitly requires it.
